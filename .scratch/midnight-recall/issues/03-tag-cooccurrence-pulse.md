# 03 — 标签共现矩阵 + 脉冲传播（联想召回）

**What to build:** 记忆开始"联想"：入库时同一文件的标签两两配对更新 `tag_cooccurrence` 表（weight +1）；召回时若启用联想（`--tag-weight > 0`），从查询中激活最匹配的标签，沿共现矩阵做广度优先脉冲传播（衰减因子、最大深度、强度阈值可配置），被传播到的标签所关联的 chunk 加入候选集，与向量相似度结果合并排序。这是"你不提关键词它也能想起来"的能力来源。

**Blocked by:** 02 — recall vector knn

**Status:** ready-for-agent

- [ ] 入库时更新 tag_cooccurrence（同文件标签两两配对，权重累加）
- [ ] 传播：激活标签 → 共现邻居扩散（衰减、深度、阈值三项参数生效）
- [ ] 联想结果与向量结果合并去重、统一排序输出
- [ ] 测试 `test_cooccurrence.py`：构造"考试↔压力"共现场景，用第三个相关词查询能经联想召回原日记，未启用联想时不召回