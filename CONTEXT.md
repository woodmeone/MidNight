# Midnight Skills — 项目上下文

> 一组受 VCP(Variable & Command Protocol) 启发但完全原创的原子化 Reasonix skill。
> 目标：给 AI 装上持久记忆和生命——记得你、会办事、不用催、懂选择。
> 创作方式：借鉴 VCP 的设计思想，完全原创实现，不复制 VCP 代码。
> 许可证：MIT（本项目原创代码，不受 VCP 的 CC BY-NC-SA 4.0 约束）

---

## 项目定位

四个原子 skill，各自解决一个 AI 的核心缺陷：

| skill | 解决的问题 | 一句话 |
|---|---|---|
| `midnight-recall` | AI 没有长期记忆，答完就忘 | 给你一个会自己写日记、自己联想想起的 AI |
| `midnight-core` | 跨端/跨会话不认得你 | 同一份事实时间线，不管在哪聊都是同一个自己 |
| `midnight-pulse` | AI 只能被动回复，不会主动行动 | 让 AI 自己定闹钟，干完活自己醒来继续 |
| `midnight-compass` | 不管什么问题都用同一个模型 | 按问题难度自动选模型，主模型挂了自动切换 |

---

## 术语表

### 核心概念

| 术语 | 定义 | 备注 |
|---|---|---|
| **Midnight Skills** | 本项目的总称，一组原子化 Reasonix skill 的集合 | 前缀 `midnight-*`，隐喻"AI 永远醒着" |
| **midnight-recall** | 记忆 skill。AI 自动写日记 + 联想式召回 + 语义检索 | 最核心的 skill，其他 skill 可能复用它的存储 |
| **midnight-core** | 跨端统一 skill。多会话/多端共享同一份事实时间线 | 不依赖 OneRing 协议，而是用时间戳文件 + 合并策略 |
| **midnight-pulse** | 自主心跳 skill。AI 自定闹钟续命，干完活自己醒来继续 | 协议简化版：`[[Pulse::Next::180]]` + `[[Pulse::Complete]]` |
| **midnight-compass** | 语义路由 skill。按问题难度自动选模型 + 容灾切换 | 可配置的模型路由表 + 向量相似度匹配 |

### 技术术语

| 术语 | 定义 |
|---|---|
| **日记文件** | AI 写入的原始文本文件，每轮对话/每次记忆事件一个文件，存在 `~/.midnight/recall/dailynote/` 下 |
| **入库** | 把日记文件切块 → 调 embedding API 算向量 → 存入 SQLite 的过程 |
| **召回** | 检索时把查询向量化 → 在 SQLite 里找最近邻 → 按共现矩阵联想扩增 → 拼入上下文 |
| **共现矩阵** | 标签之间的关联网络。两个标签经常一起出现，它们在矩阵中的连线就粗（权重高） |
| **脉冲传播** | 当某个标签被激活（用户提到它），沿共现矩阵向关联标签扩散激活信号 |
| **事实时间线** | 按时间顺序排列的跨端消息记录，每条带时间戳、来源标识、内容摘要 |
| **心跳协议** | `[[Pulse::Start]]`、`[[Pulse::Next::秒数]]`、`[[Pulse::Complete]]`、`[[Pulse::Fail]]`、`[[Pulse::Stop]]` |
| **模型路由表** | 一组预设，每条指定：模型名、触发描述语义、备选降级模型 |

---

## 设计原则

1. **完全自包含**：不依赖 VCPToolBox 进程、不依赖 VCP 服务器、任何环境都能跑。`SKILL.md` + `scripts/` 即全部。
2. **原创实现**：不复制 VCP 的 JS 代码。受设计思想启发，用 Python 重新实现核心逻辑。法律上完全干净，可自由商用。
3. **按可分发标准写**：命名、结构、文档都干净，成熟后随时可以开源或发布。
4. **零前端依赖**：所有能力通过 SKILL.md 的流程定义 + `scripts/` 的脚本实现，不依赖任何前端框架。
5. **双后端可配置**：embedding 默认用 SiliconFlow bge-m3（API 模式），留出本地模型接口，不锁死单一供应商。
6. **原子化不耦合**：四个 skill 独立可装、可升级、可只启用需要的。skill 之间通过文件约定交互（如 `midnight-core` 共享 `midnight-recall` 的 SQLite 库），不通过代码互调。

---

## 决策记录

| 决策 | 选择 | 理由 |
|---|---|---|
| 范围 | 记忆 + 跨端 + 心跳 + 路由（4 个） | 用户选定的核心能力 |
| 解耦程度 | 完全自包含 | 用户可以离开 VCP，独立运行 |
| 语言 | Python | sqlite3 标准库零安装，requests 调 API 简洁，生态成熟 |
| 存储 | SQLite 单文件 | VCP 已验证的可靠方案，零配置零服务 |
| 写日记方式 | Agent 直接写文件 + ingest 脚本 | Agent 有文件工具（write_file/bash），不需要暗号协议中转 |
| 分发 | 先自用，按可分发标准写 | 成本几乎一样，留条后路 |
| 命名 | `midnight-*` 系列 | 干净、无商标冲突、可辨识 |
| 对 VCP 代码 | 不复制，不名字关联 | 法律隔离（CC BY-NC-SA 不授权商标，商用需原创） |
| 实施顺序 | recall → core → pulse → compass | 记忆是核心也是最复杂的，先打通最难的部分 |

---

## 项目状态

- ✅ 2026-08-20：grill-with-docs 访谈完成，设计树清空
- 🚧 当前阶段：产出项目文件 + 进入 to-spec 阶段